const year=document.getElementById('year');if(year)year.textContent=new Date().getFullYear();const clock=document.getElementById('clock');const updateClock=()=>{if(clock)clock.textContent=new Intl.DateTimeFormat('de-DE',{hour:'2-digit',minute:'2-digit',second:'2-digit'}).format(new Date())};updateClock();setInterval(updateClock,1000);const pointer=document.getElementById('pointer');if(pointer&&window.matchMedia('(pointer: fine)').matches){window.addEventListener('pointermove',e=>{pointer.style.left=e.clientX+'px';pointer.style.top=e.clientY+'px'})}const navToggle=document.getElementById('navToggle');const navLinks=document.getElementById('navLinks');if(navToggle&&navLinks){navToggle.addEventListener('click',()=>navLinks.classList.toggle('open'));navLinks.querySelectorAll('a').forEach(l=>l.addEventListener('click',()=>navLinks.classList.remove('open')))}const palette=document.getElementById('palette');const openPalette=document.getElementById('openPalette');const closePalette=document.getElementById('closePalette');const showPalette=()=>{if(palette&&!palette.open)palette.showModal()};const hidePalette=()=>{if(palette&&palette.open)palette.close()};openPalette?.addEventListener('click',showPalette);closePalette?.addEventListener('click',hidePalette);window.addEventListener('keydown',e=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='k'){e.preventDefault();showPalette()}if(e.key==='Escape')hidePalette()});palette?.querySelectorAll('a').forEach(l=>l.addEventListener('click',hidePalette));const revealElements=document.querySelectorAll('.reveal');const revealObserver=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');revealObserver.unobserve(entry.target)}})},{threshold:.14});revealElements.forEach(el=>revealObserver.observe(el));document.querySelectorAll('.deck-panel,.module,.mission-card,.contact-card').forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect();card.style.setProperty('--mx',((e.clientX-r.left)/r.width)*100+'%');card.style.setProperty('--my',((e.clientY-r.top)/r.height)*100+'%')})});const copyButton=document.getElementById('copyMail');const copyHint=document.getElementById('copyHint');copyButton?.addEventListener('click',async()=>{try{await navigator.clipboard.writeText('chef@tr1stan.de');copyButton.textContent='COPIED';if(copyHint)copyHint.textContent='Kopiert. chef@tr1stan.de liegt in deiner Zwischenablage.';setTimeout(()=>{copyButton.textContent='COPY';if(copyHint)copyHint.textContent='Klick auf COPY, dann liegt die Mail in der Zwischenablage.'},2200)}catch{if(copyHint)copyHint.textContent='Kopieren ging nicht. Markier die Mail manuell.'}});const canvas=document.getElementById('matrixCanvas');const ctx=canvas?.getContext('2d');let columns=[];const chars='01<>/{}[]#$TR1STAN';const resizeCanvas=()=>{if(!canvas||!ctx)return;canvas.width=innerWidth*devicePixelRatio;canvas.height=innerHeight*devicePixelRatio;canvas.style.width=innerWidth+'px';canvas.style.height=innerHeight+'px';ctx.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);columns=Array.from({length:Math.floor(innerWidth/22)},()=>Math.random()*innerHeight)};const drawMatrix=()=>{if(!canvas||!ctx)return;ctx.fillStyle='rgba(5,7,13,.08)';ctx.fillRect(0,0,innerWidth,innerHeight);ctx.font='700 13px monospace';columns.forEach((y,i)=>{const t=chars[Math.floor(Math.random()*chars.length)];const x=i*22;ctx.fillStyle=Math.random()>.985?'rgba(120,255,214,.9)':'rgba(0,229,255,.18)';ctx.fillText(t,x,y);columns[i]=y>innerHeight+Math.random()*1000?0:y+18});requestAnimationFrame(drawMatrix)};if(canvas&&ctx&&!matchMedia('(prefers-reduced-motion: reduce)').matches){resizeCanvas();addEventListener('resize',resizeCanvas);drawMatrix()}